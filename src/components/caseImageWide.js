import styled from "styled-components"
import { theme } from "./designSystem/theme"

const CaseImageWide = styled.div`
  width: 100%;

  div {
    max-width: 1152px;
    margin: 0 5%;
  }
`

export default CaseImageWide
